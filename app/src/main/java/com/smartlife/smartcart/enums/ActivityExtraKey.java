package com.smartlife.smartcart.enums;

/**These enums are the used as keys for extras that are being passed between activities*/
public enum ActivityExtraKey {
    ORDER,
    IS_KNET,
    USER_ID,
    MEMBERSHIP_ID,
    MEMBERSHIP_NUM,
    MEMBERSHIP_BALANCE,
    CART_USER,
    USER_EMAIL
}
